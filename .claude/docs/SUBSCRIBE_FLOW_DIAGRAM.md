# Anonymous Checkout Flow - Visual Diagrams

## Complete User Journey

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ANONYMOUS CHECKOUT FLOW                             │
└─────────────────────────────────────────────────────────────────────────────┘

USER STARTS HERE
     │
     ▼
┌─────────────────────────────────────┐
│  1. Write Letter                     │
│  ┌──────────────────────────────┐  │
│  │ Title: My Future Self        │  │
│  │ Body: Dear future me...      │  │
│  │ Email: user@example.com  ◄───┼──┼─── EMAIL CAPTURED
│  │ Delivery: 1 year             │  │
│  └──────────────────────────────┘  │
│                                      │
│  [Schedule Letter] ──► PAYWALL      │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  2. /subscribe (Pricing Page)        │
│  ┌──────────────────────────────┐  │
│  │ Email: user@example.com      │  │ ◄─── PREFILLED
│  │ (locked, from letter form)   │  │
│  └──────────────────────────────┘  │
│                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐
│  │   FREE   │ │PRO MONTH │ │PRO YEAR  │
│  │  $0/mo   │ │  $9/mo   │ │ $90/year │
│  │          │ │          │ │ BEST ⭐  │
│  │[Current] │ │[Subscribe]│ │[Subscribe]│
│  └──────────┘ └──────────┘ └──────────┘
└─────────────────────────────────────┘
     │ User clicks [Subscribe]
     ▼
┌─────────────────────────────────────┐
│  3. Server Action:                   │
│     createAnonymousCheckout()        │
│  ┌──────────────────────────────┐  │
│  │ ✓ Validate email             │  │
│  │ ✓ Create Stripe Customer     │  │ ◄─── EMAIL LOCKED HERE
│  │ ✓ Create Checkout Session    │  │
│  │ ✓ Create PendingSubscription │  │
│  │   status: awaiting_payment   │  │
│  └──────────────────────────────┘  │
│                                      │
│  Returns: checkout URL               │
└─────────────────────────────────────┘
     │ Redirect to Stripe
     ▼
┌─────────────────────────────────────┐
│  4. Stripe Checkout (External)       │
│  ┌──────────────────────────────┐  │
│  │ Email: user@example.com      │  │ ◄─── LOCKED (can't edit)
│  │ [Card Number]  4242 4242...  │  │
│  │ [Expiry]       12/34          │  │
│  │ [CVC]          123            │  │
│  │                               │  │
│  │ [Pay $9.00] ──────────────►  │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
     │ Payment processed
     │
     ├────────────────┬────────────────┐
     │                │                │
     ▼                ▼                ▼
  WEBHOOK         USER PATH        RACE CONDITION
                                    HANDLING

═══════════════════════════════════════════════════════════════════════════════
WEBHOOK PATH (Background)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────┐
│  5a. Stripe Webhook                  │
│  Event: checkout.session.completed   │
│  ┌──────────────────────────────┐  │
│  │ 1. Find PendingSubscription  │  │
│  │ 2. Update status:            │  │
│  │    payment_complete          │  │
│  │ 3. Store subscriptionId      │  │
│  │ 4. Check: Does User exist?   │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
     │
     ├─── YES (User exists) ───┐
     │                          │
     │                          ▼
     │                    ┌─────────────────┐
     │                    │ AUTO-LINK NOW!  │
     │                    │ Create          │
     │                    │ Subscription    │
     │                    └─────────────────┘
     │                          │
     │                          ▼
     │                    ┌─────────────────┐
     │                    │ User gets Pro   │
     │                    │ immediately     │
     │                    └─────────────────┘
     │
     └─── NO (User doesn't exist yet) ───┐
                                          │
                                          ▼
                                    ┌─────────────────┐
                                    │ Send "Complete  │
                                    │ Signup" email   │
                                    └─────────────────┘
                                          │
                                          │ Wait for signup...

═══════════════════════════════════════════════════════════════════════════════
USER PATH (Foreground)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────┐
│  5b. User Redirected                 │
│  URL: /subscribe/success?session_id=xxx
│                                      │
│  Server Component:                   │
│  ┌──────────────────────────────┐  │
│  │ 1. Verify session with Stripe│  │
│  │ 2. Check payment_status      │  │
│  │ 3. Check: Is user authed?    │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
     │
     ├─── YES (Authenticated) ───┐
     │                            │
     │                            ▼
     │                      ┌─────────────────┐
     │                      │ Show: Success!  │
     │                      │ Subscription    │
     │                      │ Activated       │
     │                      │                 │
     │                      │ [Go to Dashboard]
     │                      └─────────────────┘
     │
     └─── NO (Not authenticated) ───┐
                                     │
                                     ▼
                               ┌─────────────────────────┐
                               │  Show: Payment Success! │
                               │  Create your account    │
                               │                         │
                               │  ┌───────────────────┐ │
                               │  │ Clerk SignUp      │ │
                               │  │ Email: user@...   │ │ ◄── LOCKED
                               │  │ (disabled)        │ │
                               │  │                   │ │
                               │  │ [Password]        │ │
                               │  │ [Verify Email]    │ │
                               │  └───────────────────┘ │
                               └─────────────────────────┘
                                     │ User signs up
                                     ▼
                               ┌─────────────────────────┐
                               │  6. Clerk Webhook        │
                               │  Event: user.created     │
                               │  ┌───────────────────┐  │
                               │  │ 1. Create User    │  │
                               │  │ 2. Check Pending  │  │
                               │  │    Subscription   │  │
                               │  │ 3. Call link()    │  │
                               │  └───────────────────┘  │
                               └─────────────────────────┘
                                     │
                                     ▼
                               ┌─────────────────────────┐
                               │  7. linkPendingSubscription()
                               │  ┌───────────────────┐  │
                               │  │ ✓ Verify email    │  │
                               │  │   verified        │  │
                               │  │ ✓ Find Pending    │  │
                               │  │   by email        │  │
                               │  │ ✓ Create Sub      │  │
                               │  │ ✓ Update Profile  │  │
                               │  │ ✓ Update Pending  │  │
                               │  │   status: linked  │  │
                               │  └───────────────────┘  │
                               └─────────────────────────┘
                                     │
                                     ▼
                               ┌─────────────────────────┐
                               │  8. Redirect to Dashboard│
                               │  ✅ Subscription Active  │
                               │  ✅ Can schedule letters │
                               └─────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
RACE CONDITION RESOLUTION (Either path wins)
═══════════════════════════════════════════════════════════════════════════════

SCENARIO A: Webhook arrives BEFORE signup
────────────────────────────────────────────
Webhook: PendingSubscription exists, User doesn't exist → Wait
User signs up → Clerk webhook → Finds PendingSubscription → Links ✓

SCENARIO B: Signup happens BEFORE webhook
────────────────────────────────────────────
User signs up → Clerk webhook → No PendingSubscription yet → Skip
Webhook arrives → Checks if User exists → YES → Auto-link ✓

BOTH PATHS WORK! 🎉
```

## Error Flows

### Error Flow 1: Payment Failed

```
┌─────────────────────────────────────┐
│  Stripe Checkout                     │
│  [Pay] ─► Card Declined ❌          │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  /subscribe/error?code=payment_failed
│  ┌──────────────────────────────┐  │
│  │ ❌ Payment Unsuccessful       │  │
│  │                               │  │
│  │ Your card was declined        │  │
│  │                               │  │
│  │ [Try Again] [Contact Support]│  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
     │ [Try Again]
     ▼
┌─────────────────────────────────────┐
│  Back to /subscribe                  │
│  Email prefilled: user@example.com   │
│  Try with different card             │
└─────────────────────────────────────┘
```

### Error Flow 2: Abandoned Checkout

```
┌─────────────────────────────────────┐
│  User at Stripe Checkout             │
│  Closes tab without paying           │
└─────────────────────────────────────┘
     │ PendingSubscription
     │ status: awaiting_payment
     │
     │ 6 hours later...
     ▼
┌─────────────────────────────────────┐
│  Inngest: send-checkout-reminder     │
│  ┌──────────────────────────────┐  │
│  │ 📧 Email to user@example.com │  │
│  │                               │  │
│  │ Subject: Complete Your        │  │
│  │          DearMe Subscription  │  │
│  │                               │  │
│  │ [Resume Checkout] ─────►      │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
     │ User clicks link
     ▼
┌─────────────────────────────────────┐
│  /subscribe/resume/[sessionId]       │
│  Redirects to Stripe Checkout        │
│  (same session, not expired)         │
└─────────────────────────────────────┘
     │ User completes payment
     ▼
  Normal flow resumes...
```

### Error Flow 3: Email Mismatch (Edge Case)

```
┌─────────────────────────────────────┐
│  Payment made with:                  │
│  payment@example.com                 │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  User tries to sign up with:         │
│  different@example.com               │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  linkPendingSubscription()           │
│  ✓ Finds no matching email           │
│  ✗ Subscription NOT linked           │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  Dashboard shows banner:             │
│  ┌──────────────────────────────┐  │
│  │ ⚠️ Email mismatch detected    │  │
│  │ Payment: payment@example.com │  │
│  │ Your email: different@...    │  │
│  │                               │  │
│  │ [Sign in with correct email] │  │
│  │ [Contact Support]            │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

## State Machine Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│               PENDING SUBSCRIPTION STATE MACHINE                 │
└─────────────────────────────────────────────────────────────────┘

                    [Initial State]
                         │
                         │ createAnonymousCheckout()
                         ▼
              ┌──────────────────────┐
              │  awaiting_payment    │
              └──────────────────────┘
                         │
            ┌────────────┴────────────┐
            │                         │
            │ checkout.session        │ 24h timeout
            │ .completed             │
            ▼                         ▼
  ┌──────────────────────┐   ┌──────────────────────┐
  │  payment_complete    │   │      expired         │
  └──────────────────────┘   └──────────────────────┘
            │                         │
            │                         │ cleanup job
            │ linkPending             │ (refund if paid)
            │ Subscription()          │
            ▼                         ▼
  ┌──────────────────────┐   ┌──────────────────────┐
  │      linked          │   │      refunded        │
  │  (TERMINAL STATE)    │   │  (TERMINAL STATE)    │
  └──────────────────────┘   └──────────────────────┘
            │
            │ User can now
            │ use subscription
            ▼
      [Dashboard Access]


ALLOWED TRANSITIONS:
  awaiting_payment → payment_complete  ✓
  awaiting_payment → expired           ✓
  payment_complete → linked            ✓
  payment_complete → expired           ✓ (30-day timeout)
  expired → refunded                   ✓ (if payment exists)

BLOCKED TRANSITIONS:
  linked → any state                   ✗ (immutable)
  refunded → any state                 ✗ (immutable)
  payment_complete → awaiting_payment  ✗ (backwards)
```

## Database Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                     DATABASE SCHEMA OVERVIEW                     │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  PendingSubscription │
├──────────────────┤
│ id               │ UUID (PK)
│ email            │ Citext ◄────────┐
│ stripeCustomerId │ String (Unique) │
│ stripeSessionId  │ String (Unique) │
│ stripeSubscriptionId │ String?     │
│ priceId          │ String          │
│ plan             │ Enum            │
│ amountCents      │ Int             │
│ status           │ Enum            │
│ expiresAt        │ DateTime        │
│ linkedUserId     │ UUID? (FK)      │
└──────────────────┘                 │
         │                           │
         │ After payment             │
         │ & signup                  │
         ▼                           │
    PROMOTES TO                      │
         │                           │
         ▼                           │
┌──────────────────┐                 │
│  Subscription    │                 │
├──────────────────┤                 │
│ id               │ UUID (PK)       │
│ userId           │ UUID (FK) ──────┤───► User.id
│ stripeSubscriptionId │ String      │
│ status           │ Enum            │
│ plan             │ Enum            │
│ currentPeriodEnd │ DateTime        │
└──────────────────┘                 │
                                     │
┌──────────────────┐                 │
│  User            │                 │
├──────────────────┤                 │
│ id               │ UUID (PK) ◄─────┘
│ clerkUserId      │ String (Unique)
│ email            │ Citext (Unique) ◄── LINKING KEY
├──────────────────┤
│ profile          │ Relation
│ subscriptions    │ Relation
│ letters          │ Relation
└──────────────────┘
         │
         ▼
┌──────────────────┐
│  Profile         │
├──────────────────┤
│ userId           │ UUID (PK, FK)
│ stripeCustomerId │ String? (Unique)
│ timezone         │ String
└──────────────────┘

KEY RELATIONSHIPS:
─────────────────────────────────────────────────────────
PendingSubscription.email  =  User.email  (Linking Key)
PendingSubscription.linkedUserId → User.id (After linking)
Subscription.userId → User.id
Profile.stripeCustomerId = PendingSubscription.stripeCustomerId (After linking)
```

## Timeline View: 30-Day Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│           PENDING SUBSCRIPTION LIFECYCLE TIMELINE                │
└─────────────────────────────────────────────────────────────────┘

Day 0
─────
  00:00  User creates checkout
         PendingSubscription created (status: awaiting_payment)

  00:15  User completes payment
         Webhook: status → payment_complete
         Email sent: "Complete Signup"

Day 1
─────
  00:15  Reminder email: "Activate Your Subscription"
         (24 hours after payment)

Day 7
─────
  00:15  Warning email: "Expiring in 23 days"

Day 14
──────
  00:15  Warning email: "Expiring in 16 days"

Day 23
──────
  00:15  Final warning: "Expiring in 7 days!"

Day 30
──────
  00:15  PendingSubscription expires

  02:00  Cleanup cron job runs:
         - Cancels Stripe subscription
         - Issues refund
         - Updates status: refunded
         - Sends email: "Subscription Expired & Refunded"

HAPPY PATH (User signs up on Day 0):
────────────────────────────────────
Day 0
  00:00  Checkout created
  00:15  Payment completed
  00:16  User signs up
  00:17  Subscription linked ✅
  ────► No reminder emails sent
  ────► User has active subscription
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    COMPONENT STRUCTURE                           │
└─────────────────────────────────────────────────────────────────┘

app/subscribe/
├── page.tsx (Server Component)
│   ├─► Fetches PricingPlans from DB
│   ├─► Detects email from query param
│   ├─► Checks for incomplete/complete payments
│   └─► Renders:
│       ├─► EmailCaptureForm (if needed)
│       ├─► SubscriptionStatusBanner
│       └─► PricingGrid
│
├── layout.tsx (Minimal Layout)
│   └─► No header/footer (as required)
│
├── actions.ts (Server Actions)
│   ├─► createAnonymousCheckout()
│   └─► resumeCheckout()
│
├── success/
│   └── page.tsx (Server Component)
│       ├─► Verifies session with Stripe
│       ├─► Detects authentication status
│       └─► Renders:
│           ├─► SuccessMessage (if authenticated)
│           └─► SuccessSignupForm (if not)
│
├── error/
│   └── page.tsx (Server Component)
│       └─► Renders error based on code
│
└── resume/
    └── [sessionId]/
        └── page.tsx (Server Component)
            └─► Redirects to Stripe Checkout

components/subscribe/
├── pricing-card.tsx (Client)
│   └─► Individual pricing card with animations
│
├── pricing-grid.tsx (Server)
│   └─► Layout for pricing cards
│
├── email-capture-form.tsx (Client)
│   └─► Email input with validation
│
├── checkout-button.tsx (Client)
│   ├─► Calls createAnonymousCheckout()
│   └─► Shows loading state
│
├── success-signup-form.tsx (Client)
│   └─► Clerk SignUp with locked email
│
└── subscription-status-banner.tsx (Client)
    └─► Shows incomplete/complete payment status

SERVER ACTIONS:
─────────────────
createAnonymousCheckout()  → Checkout URL
linkPendingSubscription()  → Link result
resumeCheckout()           → Resume URL

WEBHOOKS:
─────────────────
/api/webhooks/stripe  → Enhanced with PendingSubscription logic
/api/webhooks/clerk   → Enhanced with auto-linking

CRON JOBS:
─────────────────
/api/cron/cleanup-pending-subscriptions
/api/cron/send-checkout-reminders
/api/cron/send-expiry-warnings

INNGEST FUNCTIONS:
─────────────────
billing/send-checkout-reminder
billing/send-signup-reminder
billing/send-expiry-warning
```

---

## Quick Reference: Key Functions

### createAnonymousCheckout()
```typescript
Input:  { email, priceId, letterId? }
Output: { sessionUrl, sessionId, customerId }
Flow:   Validate → Check existing → Create Stripe Customer
        → Create Stripe Checkout → Create PendingSubscription
```

### linkPendingSubscription()
```typescript
Input:  { userId }
Output: { success, subscriptionId?, error? }
Flow:   Get user email → Find PendingSubscription by email
        → Verify email verified → Create Subscription
        → Update Profile → Update PendingSubscription (linked)
```

### Webhook: checkout.session.completed
```typescript
Flow:   Find PendingSubscription → Update status (payment_complete)
        → Check if User exists:
          YES → Auto-link immediately
          NO  → Send signup reminder email
```

### Clerk Webhook: user.created
```typescript
Flow:   Create User in DB → Check for PendingSubscription
        → If found: Call linkPendingSubscription()
```

---

## Security Checklist

```
✅ Email locked in Stripe Checkout (customer created first)
✅ Email verification required before activation (Clerk)
✅ Idempotent webhook processing (event.id in WebhookEvent table)
✅ Idempotent subscription linking (safe to call multiple times)
✅ Race condition handling (dual-path resolution)
✅ Session expiry (24 hours for checkout)
✅ Payment expiry (30 days for activation)
✅ Audit trail (all events logged)
✅ CRON_SECRET for cron endpoints
✅ Stripe webhook signature verification
```

---

**See full documentation**: `ANONYMOUS_CHECKOUT_DESIGN.md`
**Implementation checklist**: `SUBSCRIBE_IMPLEMENTATION_CHECKLIST.md`
